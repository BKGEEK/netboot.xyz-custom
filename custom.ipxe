#!ipxe
:start
echo Downloading and loading ISO into memory...
initrd https://repo.virtuozzo.com/vzlinux/8.4/iso/vzlinux-8-min.iso || goto failed
echo ISO loaded. Starting memdisk boot...
chain http://preseed.panticz.de/ipxe/memdisk iso raw || goto failed
:failed
echo Boot failed! Check network, ISO size, or memdisk availability.
sleep 5
exit
