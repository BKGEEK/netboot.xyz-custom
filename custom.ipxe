#!ipxe
:start
echo Starting SAN boot for ISO...
set iso_url https://repo.virtuozzo.com/vzlinux/8/iso/vzlinux-8-min.iso  # 替换为你的 ISO URL
sanboot --no-describe ${iso_url} || goto failed
:failed
echo SAN boot failed! Check network or ISO URL.
sleep 5
exit
